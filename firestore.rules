rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // The frontend only communicates to Next.js API Routes.
      // API Routes utilize Firebase Admin SDK which securely bypasses these rules.
      // Therefore, all direct client access is locked down.
      allow read, write: if false;
    }
  }
}
